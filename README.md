# data-quality
Data Quality is a rule evaluation engine based on Apache spark. User can evaluate configured rules against an spark dataset and generate evaluation result which would contain row wise result of evaluation as success and failure
